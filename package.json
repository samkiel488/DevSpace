{
  "name": "devspace-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd Frontend && npm install && npm run build && cd ../Backend && npm install",
    "start": "node Backend/index.js",
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && nodemon index.js\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
