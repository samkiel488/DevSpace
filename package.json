{
  "name": "devspace-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
"postinstall": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install",
    "start": "node backend/index.js",
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && nodemon index.js\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
